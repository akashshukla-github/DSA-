//          JAI SHREE RAM          //

#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<int> v; // you need not mention size
    v.push_back(6);

    v.push_back(5);

    v.push_back(9);

    v.push_back(7);

    cout << v[0] << " ";
    cout << v[1] << " ";
    cout << v[2] << " ";
    cout << v[3] << " ";

    return 0;
}
