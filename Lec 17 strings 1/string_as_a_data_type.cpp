//          JAI SHREE RAM          //

#include <iostream>

using namespace std;

int main()
{
    // string str = "akash shukla";
    // cout << str[8];
    string s;
    // cin >> s;// only if the given string has no spaces
    getline(cin,s);// if given string has spaces
    cout << s;

    return 0;
}
